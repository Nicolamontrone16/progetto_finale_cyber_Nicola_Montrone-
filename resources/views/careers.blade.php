<x-layout title="Collabora con noi">
    <section class="content-section"><div class="container"><x-page-header eyebrow="Community" title="Costruiamo insieme contenuti migliori" description="Candidati per contribuire al progetto con il ruolo più adatto alle tue competenze." />
        <div class="career-grid"><form action="{{ route('careers.submit') }}" method="POST" class="surface-card career-form">@csrf<h2>Invia la tua richiesta</h2><p>La candidatura sarà valutata da un amministratore.</p><div class="form-group"><label for="role">Ruolo richiesto</label><select name="role" id="role" class="form-select @error('role') is-invalid @enderror" required><option value="" selected disabled>Seleziona un ruolo</option>@if(!Auth::user()->is_admin)<option value="admin">Amministratore</option>@endif @if(!Auth::user()->is_revisor)<option value="revisor">Revisore</option>@endif @if(!Auth::user()->is_writer)<option value="writer">Writer</option>@endif</select>@error('role')<span class="field-error">{{ $message }}</span>@enderror</div><div class="form-group"><label for="email">Email</label><input type="email" class="form-control @error('email') is-invalid @enderror" id="email" name="email" value="{{ old('email', Auth::user()->email) }}" required>@error('email')<span class="field-error">{{ $message }}</span>@enderror</div><div class="form-group"><label for="message">Motivazione</label><textarea name="message" id="message" rows="7" class="form-control @error('message') is-invalid @enderror" placeholder="Raccontaci come vorresti contribuire…" required>{{ old('message') }}</textarea>@error('message')<span class="field-error">{{ $message }}</span>@enderror</div><button type="submit" class="btn btn-primary">Invia candidatura</button></form>
            <div class="role-list"><article class="role-card"><span>01</span><div><h2>Writer</h2><p>Scrivi analisi, guide e notizie da sottoporre alla revisione editoriale.</p></div></article><article class="role-card"><span>02</span><div><h2>Revisore</h2><p>Verifica qualità e affidabilità dei contenuti prima della pubblicazione.</p></div></article><article class="role-card"><span>03</span><div><h2>Amministratore</h2><p>Gestisci ruoli e struttura editoriale attraverso gli strumenti protetti.</p></div></article></div>
        </div>
    </div></section>
</x-layout>
